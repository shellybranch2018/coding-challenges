const assert = require("chai").assert;
const largestProduct = require("../src/largest-product");

describe("largestProduct", function() {
  it('Accepts an array of integers and returns the largest product possible from three of those numbers', function() {
	   
	 
  });

});

